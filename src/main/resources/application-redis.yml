spring:
  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379
      connect-timeout: 60000
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 2
